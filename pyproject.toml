[project]
version = "0.0.0"
name = "resample3"

[build-system]
requires = ["setuptools>=61", "wheel", "numpy>=1.21"]
build-backend = "setuptools.build_meta"

[project.optional-dependencies]
demos = ["h5gizmos"]
